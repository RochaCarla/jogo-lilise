import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.lilise.casacriativa',
  appName: 'Casa Criativa',
  webDir: 'dist',
};

export default config;
